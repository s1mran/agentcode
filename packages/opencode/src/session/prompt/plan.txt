<system-reminder>
Plan mode is still active: stay read-only. Do not edit files or change the system; the plan file ${planPath} is the only file that may be edited.
Read-only shell commands run without approval; any other command needs the user's approval, so never use the shell to modify anything.
Subagents run read-only in plan mode; use them only to explore and review.
End your turn by asking the user a question or, once the plan is final, ${exitInfo}.
</system-reminder>
